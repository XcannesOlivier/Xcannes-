/**
 * Shared design tokens for demo-wallet modals.
 * Re-exports the canonical tokens from the wallet modals directory
 * so demo-wallet imports stay short and colocated.
 */
export {
  greenActionBtnBase,
  greenActionBtnMuted,
  MODAL_MAX_H,
} from "../../wallet/modals/walletModalTokens";
